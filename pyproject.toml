[project]
name = "training-tests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.1",
    "pillow>=12.1.0",
    "pytest>=9.0.2",
    "pytest-django>=4.11.1",
]
